% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{format_query_result}
\alias{format_query_result}
\title{Format pipeline output for direct query mode}
\usage{
format_query_result(df, original_col = "taxon_clean")
}
\arguments{
\item{df}{A data frame returned by any pipeline resolution function.}

\item{original_col}{Character. The original taxon column name supplied
by the user (stored in \code{attr(df, "original_col")}). Used to rename
the output column back to what the user expects. Default: \code{"taxon_clean"}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns: the taxon column (renamed to
\code{original_col}), \code{matched_name}, \code{accepted_name}, \code{taxonomic_status},
\code{resolution_method}, and \code{resolution_notes}.
}
\description{
Selects and returns only the most relevant columns from a resolved
data frame when pipeline functions are called with a bare character
vector instead of a full data frame. Keeps the output readable in
an interactive session without exposing the 30+ taxonomy rank columns
that are mostly \code{NA} at query time.
}
\keyword{internal}
