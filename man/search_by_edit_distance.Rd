% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuzzy.R
\name{search_by_edit_distance}
\alias{search_by_edit_distance}
\title{Correct genus via edit-distance against a vocabulary}
\usage{
search_by_edit_distance(
  taxon_clean,
  vocab,
  max_dist = 3,
  max_candidates = 15,
  silent = FALSE,
  timeout_sec = 10
)
}
\arguments{
\item{taxon_clean}{Character. The cleaned taxon name to correct.}

\item{vocab}{Character vector of known genus names.}

\item{max_dist}{Integer. Maximum Levenshtein distance. Default \code{3}.}

\item{max_candidates}{Integer. Maximum genus candidates to test. Default \code{15}.}

\item{silent}{Logical. Suppress progress messages. Default \code{FALSE}.}

\item{timeout_sec}{Timeout for WoRMS REST calls. Default \code{10}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{variant}, \code{aphiaid}, \code{dist},
\code{method}, or \code{NULL} if no valid match found.
}
\description{
Finds genus names in \code{vocab} within Levenshtein distance <= \code{max_dist}
of the input genus, builds full candidate names (genus + epithet), and
validates each against WoRMS via the REST API.
}
\keyword{internal}
