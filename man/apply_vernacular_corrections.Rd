% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vernacular.R
\name{apply_vernacular_corrections}
\alias{apply_vernacular_corrections}
\title{Apply vernacular corrections to taxon_clean}
\usage{
apply_vernacular_corrections(x, col = "taxon_clean")
}
\arguments{
\item{x}{A data frame with a \code{taxon_clean} column, or a bare
character vector of cleaned taxon names.}

\item{col}{Character. Name of the column to correct when \code{x} is a
data frame. Default: \code{"taxon_clean"}.}
}
\value{
The same object as \code{x} with corrections applied where
patterns matched.
}
\description{
Iterates over the \code{taxon_clean} column of a data frame (or a bare
character vector) and replaces any pattern from the vernacular dictionary
found at the START of the string (case-insensitive, longest key first)
with its canonical form, preserving any trailing epithet.
}
\details{
This is meant to be called as the LAST step of the cleaning pipeline
(Stage 1), so that \code{taxon_clean} is already correct before automatic
resolution begins.

When \code{x} is a data frame and the target \code{col} is missing but
equals the default \code{"taxon_clean"}, the function falls back to
initialising it from \code{taxon} (if present), so that it can also be
called in isolation on a raw data frame without requiring the full
cleaning pipeline to have run first.

Example: \code{"pseudo nitzschia delicatissima"} ->
\code{"Pseudo-nitzschia delicatissima"}
}
