% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuzzy.R
\name{process_fuzzy_batch}
\alias{process_fuzzy_batch}
\title{Run the interactive semi-automatic fuzzy resolution loop}
\usage{
process_fuzzy_batch(
  df,
  genus_vocab,
  epithet_vocab,
  col = "taxon_clean",
  batch_size = 10,
  checkpoint_file = "phytaxr_step6_checkpoint.rds",
  min_similarity = 0.85,
  max_suggestions = 15,
  max_longshot_suggestions = 10,
  longshot_threshold = 0.3,
  edit_max_dist = 3,
  edit_max_candidates = 15,
  timeout_sec = 15
)
}
\arguments{
\item{df}{A data frame with the resolution schema (as produced by the
cleaning + automatic resolution pipeline).}

\item{genus_vocab}{Character vector from \code{\link[=build_genus_vocabulary]{build_genus_vocabulary()}}.}

\item{epithet_vocab}{Character vector from \code{\link[=build_epithet_vocabulary]{build_epithet_vocabulary()}}.}

\item{col}{Character. Name of the column to use as the cleaned taxon name
source. Default \code{"taxon_clean"}.}

\item{batch_size}{Integer. Number of entries to process before pausing for
continuation confirmation. Default \code{10}.}

\item{checkpoint_file}{Character. Path to an RDS file used to save and
resume progress between sessions. After each accepted resolution the
current state is written to this file. If the file already exists when
the function is called, it is loaded automatically and processing
resumes from where it left off. Set to \code{NULL} to disable checkpointing.
Default \code{"phytaxr_step6_checkpoint.rds"}.}

\item{min_similarity}{Minimum similarity for "good" suggestions. Default \code{0.85}.}

\item{max_suggestions}{Maximum good suggestions to display. Default \code{15}.}

\item{max_longshot_suggestions}{Maximum longshot suggestions. Default \code{10}.}

\item{longshot_threshold}{Minimum similarity for longshot bucket. Default \code{0.30}.}

\item{edit_max_dist}{Maximum Levenshtein distance for edit-distance steps.
Default \code{3}.}

\item{edit_max_candidates}{Maximum genus candidates in edit-distance steps.
Default \code{15}.}

\item{timeout_sec}{Timeout for WoRMS calls in seconds. Default \code{15}.}
}
\value{
The updated data frame.
}
\description{
Iterates over all unresolved entries in \code{df} (where \code{matched_aphiaid} is
\code{NA} and \code{resolution_method} is not \code{"expert_review"}), presents cascading
WoRMS suggestions via \code{\link[=search_worms_fuzzy_suggestions]{search_worms_fuzzy_suggestions()}}, collects user
choices via \code{\link[=prompt_fuzzy_suggestions]{prompt_fuzzy_suggestions()}}, and writes accepted resolutions
back to \code{df}. Progress is saved to \code{.GlobalEnv} after each accepted entry.
}
