% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{op-null-default}
\alias{\%||\%}
\title{Null-coalescing operator}
\usage{
lhs \%||\% rhs
}
\arguments{
\item{lhs}{An object. If not \code{NULL}, it is returned as-is.}

\item{rhs}{Fallback value returned when \code{lhs} is \code{NULL}.}
}
\value{
\code{lhs} if \code{!is.null(lhs)}, otherwise \code{rhs}.
}
\description{
Returns \code{lhs} if not \code{NULL}, otherwise returns \code{rhs}.
Syntactic sugar for the common \code{if (!is.null(x)) x else default}
pattern.
}
\details{
\strong{Important:} only \code{NULL} triggers the fallback. \code{NA}
is \emph{not} \code{NULL}, so \code{NA \%||\% "fallback"} returns
\code{NA}, not \code{"fallback"}. Use \code{\link[base]{is.na}()} or
\code{\link[base]{replace}()} to handle \code{NA} coalescing separately
if needed.
}
\examples{
# NULL on the left-hand side triggers the fallback
NULL %||% "default"   # "default"

# A real value passes straight through
"real" %||% "default"   # "real"

# NA is NOT NULL: it passes through unchanged
NA %||% "default"   # NA  (not "default")
is.na(NA %||% "default")   # TRUE
}
\keyword{internal}
