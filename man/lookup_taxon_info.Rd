% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{lookup_taxon_info}
\alias{lookup_taxon_info}
\title{Look up a taxon name in a pre-loaded dictionary}
\usage{
lookup_taxon_info(search_term, lookup, include_notes = TRUE)
}
\arguments{
\item{search_term}{Character. The taxon name to look up.}

\item{lookup}{A named list where each name is a taxon string and each
value is a one-row data frame with resolution columns.}

\item{include_notes}{Logical. Whether to include the \code{marginalia}
column. Default: \code{TRUE}.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{matched_aphiaid},
\code{matched_name}, \code{accepted_name}, \code{accepted_aphiaid},
\code{taxonomic_status}, \code{resolution_method}, \code{aphiaid}, \code{marginalia},
and \code{found}.
}
\description{
Searches a named list (dictionary) for a taxon name and returns
resolution metadata. Returns a tibble with \code{found = FALSE} if the
name is not present.
}
