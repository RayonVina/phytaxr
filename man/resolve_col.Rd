% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{resolve_col}
\alias{resolve_col}
\title{Resolve and normalise the taxon column inside a data frame}
\usage{
resolve_col(df, col = "taxon_clean")
}
\arguments{
\item{df}{A data frame.}

\item{col}{A character string with the name of the column that contains
taxon names. Default: \code{"taxon_clean"}.}
}
\value{
\code{df} with the taxon column renamed to \code{taxon_clean} (if
necessary), and attribute \code{"original_col"} set to \code{col}.
}
\description{
Validates that the user-supplied column name exists in the data frame
and, if it differs from \code{taxon_clean}, renames it to \code{taxon_clean} so
the rest of the pipeline can work with a single known column name.
The original column name is stored in the attribute \code{"original_col"}
of the returned data frame so it can be restored later if needed.
}
\keyword{internal}
