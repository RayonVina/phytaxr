% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuzzy.R
\name{search_worms_fuzzy_suggestions}
\alias{search_worms_fuzzy_suggestions}
\title{Search WoRMS with cascading fuzzy strategies}
\usage{
search_worms_fuzzy_suggestions(
  taxon_clean,
  original_taxon,
  genus_vocab,
  epithet_vocab,
  min_similarity = 0.85,
  max_suggestions = 15,
  max_longshot_suggestions = 10,
  longshot_threshold = 0.3,
  edit_max_dist = 3,
  edit_max_candidates = 15,
  timeout_sec = 15
)
}
\arguments{
\item{taxon_clean}{Character. Cleaned taxon name to search.}

\item{original_taxon}{Character. Original verbatim name (for display).}

\item{genus_vocab}{Character vector from \code{\link[=build_genus_vocabulary]{build_genus_vocabulary()}}.}

\item{epithet_vocab}{Character vector from \code{\link[=build_epithet_vocabulary]{build_epithet_vocabulary()}}.}

\item{min_similarity}{Minimum similarity score for "good" suggestions.
Default \code{0.85}.}

\item{max_suggestions}{Maximum number of good suggestions to return.
Default \code{15}.}

\item{max_longshot_suggestions}{Maximum number of longshot suggestions.
Default \code{10}.}

\item{longshot_threshold}{Minimum similarity for longshot bucket. Default \code{0.30}.}

\item{edit_max_dist}{Maximum Levenshtein distance for edit-distance steps.
Default \code{3}.}

\item{edit_max_candidates}{Maximum genus candidates in edit-distance steps.
Default \code{15}.}

\item{timeout_sec}{Timeout for WoRMS REST calls in seconds. Default \code{15}.}
}
\value{
A named list with elements \code{good}, \code{longshot}, \code{genus_fallback},
and \code{binomial_fallback} (each a tibble or \code{NULL}).
}
\description{
Orchestrates a multi-level cascade of WoRMS search strategies for a single
taxon name that could not be resolved automatically:
}
\details{
\itemize{
\item \strong{L1}: Full name, taxamatch + REST fuzzy
\item \strong{L1b}: Edit-distance genus correction (local vocabulary)
\item \strong{L1c}: Token-level edit-distance genus + epithet (local vocabulary)
\item \strong{L1d}: WoRMS fuzzy genus reconstruction (remote, genera not in vocab)
\item \strong{L1e}: Dehyphenated epithet
\item \strong{L2}: Truncate last token stem
\item \strong{L3}: Truncate last + second token (trinomials)
\item \strong{L4}: Drop suffix, search binomial (trinomials)
\item \strong{L5}: Drop suffix + truncate epithet (trinomials)
\item \strong{L6}: Genus only (always, shown as LTR fallback)
}

Returns consolidated, ranked suggestions grouped by confidence tier.
}
