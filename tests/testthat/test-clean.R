# Tests for clean_taxon_name() removed: function was deprecated and removed
# from the package. Cleaning is now handled by the modular pipeline functions
# (normalize_characters, process_sp_entries, etc.) called from the user script.
